# Purpose

This Flask script will mock the student payment API.

It returns static content and must be edited to change its responses.

# Usage

	$ virtualenv venv
	$ . venv/bin/activate
	$ pip install -r requirements.txt
	$ ./student_pay_api.py

